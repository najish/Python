
def merge(list,left,right):
    if left < right:
        mid = left + (right - left) // 2
        list1 = merge(list,left,mid)
        list2 = merge(list,mid+1,right)



def mergeList(merge,left,right)

if __name__ == "__main__":
    pass